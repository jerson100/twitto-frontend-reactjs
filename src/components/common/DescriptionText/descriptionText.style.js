import styled from "styled-components";

const DescriptionTextStyle = styled.div`
  color: ${({ theme }) => theme.COLORS.SECONDARY};
  font-weight: normal !important;
`;

export { DescriptionTextStyle };
