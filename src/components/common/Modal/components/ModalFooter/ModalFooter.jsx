import React from "react";
import { ModalFooterStyle } from "../../modal.style";

const ModalFooter = ({ children }) => {
  return <ModalFooterStyle>{children}</ModalFooterStyle>;
};

export default ModalFooter;
