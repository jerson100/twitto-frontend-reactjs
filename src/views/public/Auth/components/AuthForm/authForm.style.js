import styled from "styled-components";
const RegisterTermsStyle = styled.div`
  color: ${({ theme }) => theme.COLORS.SECONDARY};
`;
const AuthFormStyle = styled.div`
  max-width: 300px;
`;

export { RegisterTermsStyle, AuthFormStyle };
